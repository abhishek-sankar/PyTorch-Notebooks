import React from "react";

export const ElizaLogoText = ({ className }: { className?: string }) => {
  return (
    <div className={`font-bold text-primary ${className}`}>
      ELIZA
    </div>
  );
};